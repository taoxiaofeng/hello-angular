import { IconSvgHtmlPipe } from './icon-svg-html.pipe';

describe('IconSvgHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new IconSvgHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
